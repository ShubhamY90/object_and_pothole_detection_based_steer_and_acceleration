# Core CV / DL
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0    # DeepLabV3 road segmentation model
opencv-python>=4.8.0
numpy>=1.24.0
# timm>=0.9.0          # only needed for MiDaS DPT_Large (no longer used)
